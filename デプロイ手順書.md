# SC Hearing System デプロイスクリプト

## 📦 ファイル構成

| ファイル | 用途 |
|---|---|
| **deploy-aws.ps1** | メインスクリプト (フル版・全機能) |
| **deploy-aws-fast.ps1** | 高速版ラッパー (screen-viewer 除外) |

両方を **同じフォルダ** に配置してください。例: `C:\Projects\SC-HearingSystem\`

## 🚀 通常使用 (推奨)

screen-viewer は変更頻度が低いので、普段は **高速版** を使う：

```powershell
cd C:\Projects\SC-HearingSystem
.\deploy-aws-fast.ps1
```

転送量が大幅減少。ビルド + 転送 + デプロイで **約1〜2分** で完了します。

## 🔄 screen-viewer も更新するとき

screen-viewer の中身を更新したいときだけ、フル版を使う：

```powershell
.\deploy-aws.ps1
```

## ⚙️ オプション一覧

両スクリプトで使えるオプション：

| オプション | 効果 |
|---|---|
| `-FrontendOnly` | フロントエンドのみデプロイ (バックエンド未変更時) |
| `-BackendOnly` | バックエンドのみデプロイ (フロント未変更時) |
| `-SkipBuild` | ビルドをスキップして既存の dist/publish を使う |

`deploy-aws.ps1` のみ:

| オプション | 効果 |
|---|---|
| `-SkipScreenViewer` | screen-viewer 除外 (= deploy-aws-fast.ps1 と同じ) |

### 使用例

```powershell
# よく使うパターン

# 通常デプロイ (高速版)
.\deploy-aws-fast.ps1

# フロントエンドだけ素早くデプロイ
.\deploy-aws-fast.ps1 -FrontendOnly

# バックエンドだけ
.\deploy-aws-fast.ps1 -BackendOnly

# 既にビルド済みで、デプロイだけしたい
.\deploy-aws-fast.ps1 -SkipBuild

# screen-viewer も含めて完全更新
.\deploy-aws.ps1
```

## 🔧 内部処理

### 高速化の仕組み
1. **tar+gzip 圧縮**: 数百ファイルを1ファイルに圧縮 → scp は1回だけ
2. **screen-viewer 除外**: 大量の静的ファイル群を転送対象から除外
3. **rsync 不要**: PowerShell 標準機能だけで完結

### EC2側の処理 (高速版時)
```bash
# screen-viewer を保護したまま、それ以外を削除して上書き
find /var/www/sc-hearing/wwwroot -mindepth 1 -maxdepth 1 \
  ! -name 'screen-viewer' -exec sudo rm -rf {} +
sudo cp -r /tmp/web-update/* /var/www/sc-hearing/wwwroot/
```

これにより screen-viewer は EC2 上で**保持されたまま**、それ以外のファイルが新しくなります。

### EC2側の処理 (フル版時)
```bash
# wwwroot を全削除してすべて新しい dist で置き換え
sudo rm -rf /var/www/sc-hearing/wwwroot/*
sudo cp -r /tmp/web-update/* /var/www/sc-hearing/wwwroot/
```

## ⚠️ 設定変更が必要な場合

スクリプト内の以下のパスが環境と違う場合は、`deploy-aws.ps1` の冒頭部分を修正してください：

```powershell
$KEY         = "D:\TEMP\AWS\tcs_dev_2023.pem"          # SSH秘密鍵
$EC2         = "ec2-user@54.64.46.6"                    # EC2接続先
$API_DIR     = "C:\Projects\SC-HearingSystem\SCHearing.API"
$WEB_DIR     = "C:\Projects\SC-HearingSystem\sc-hearing-web"
$REMOTE_BASE = "/var/www/sc-hearing"                    # サーバ側配置先
```

## 🆘 トラブルシューティング

### "execution policy" エラー

```powershell
# 現在のセッションだけ許可
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\deploy-aws-fast.ps1
```

### "tar: command not found"

Windows 10 (1803以降) には `tar.exe` が標準搭載されています。
古い Windows の場合は別途インストールが必要です。

### "scp/ssh: command not found"

OpenSSH クライアントが必要です：
```powershell
Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
```

### サービスが起動しない

エラーログ確認：
```powershell
ssh -i D:\TEMP\AWS\tcs_dev_2023.pem ec2-user@54.64.46.6 "sudo journalctl -u sc-hearing -n 50"
```
